surface pumpkinPattern (
	color Cin = 1,
output color Cout=0, output float a = 1, output float b = 0)
{

		color green = color(0,0.4,0);
		a = 0.8*(abs(0.2*sin(10*u*M_PI))+0.3*noise(u*10)+0.04*noise("usimplex",u*40, v*7));
		Cout = Cin*a;
		if(v < .06){
			color grey = color(0.2,0.2,0.2);
			Cout = grey;
		}
		else if( v > 0.07){
	    float mag=noise(P*200);
	    b=smoothstep(0.6,1,mag)*0.4;
			Cout = mix(Cout, green, 0.2*v);
		}

}
