displacement dis(float disp = 0.25; float pin = 0.1)
{

    float scale;
    float ret=evalparam("disp",scale);
    float pinprick;
    float ret2=evalparam("pin",pinprick);
    P += normalize(N) * scale * 0.25;
    P -= normalize(N) * pinprick*0.003;
    if(v < .05){
      P+= vector (0,0.01,0);
      P+= normalize(N)*0.05;
    }
		P -= normalize(N) * sin((1-v));

}
