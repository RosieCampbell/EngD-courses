displacement dis(float disp = 0.25; float atten = 0.1)
{

    //float seg =0.7*sin(u*20*PI)+0.02*sin(u*160*PI)+0.05*sin(u*75*PI);
    //float noi = 1.3*noise(u*10)+0.5*noise(u*20)+0.01*noise(u*300);
    float scale;
    float ret=evalparam("disp",scale);
    P += normalize(N) * scale * 0.25;

    if(v < .05){
      P+= vector (0,0.02,0);
      P+= normalize(N)*0.05;
    }
		P -= normalize(N) * sin((1-v));

}
